# Einlesen der CSV-Daten
broadband_data <- read.csv("data/breitbandzugang.csv")
spending_data <- read.csv("data/haushaltsausgaben.csv")

# Anzeigen der ersten Zeilen, um die Struktur zu überprüfen
head(broadband_data)
head(spending_data)

